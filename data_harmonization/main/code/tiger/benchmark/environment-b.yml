name: data-harmonization-benchmark

channels:
  - https://conda.anaconda.org/conda-forge

dependencies:
  - python=3.9
  - pip
  - numpy
  - pandas
  - xlrd
  - pip:
      - dedupe
      - pandas-dedupe
      - unidecode
      - dedupe-hcluster
      - rlr

