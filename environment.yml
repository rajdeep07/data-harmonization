name: data-harmonization

channels:
  - https://conda.anaconda.org/conda-forge
dependencies:
# conda installs should be used where possible especially for any packages that need to be compiles
  - python=3.9
  - numpy  # can lock down versions if required
  - pandas
  - matplotlib
  - pip
  - pytest
  - pytest-cov
  - pyarrow
  - scipy
  - statsmodels
  - python-dotenv
  - configparser
  - dataclasses
  - python-levenshtein
  - metaphone
#  - clean-text
  - pip:
    - sphinx-autodoc-annotation
    - datamodel-code-generator
    - pydantic